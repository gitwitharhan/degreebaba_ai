Reusable React components live here.

Current MVP keeps most parser-specific UI inside `src/pages/ParserPage.jsx` because the workflow is tightly coupled. As the app grows, move shared pieces such as upload cards, validation panels, and JSON viewers into this folder.
